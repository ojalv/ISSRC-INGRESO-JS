/*47. Crea una variable llamada `precioProducto` y otra llamada `cantidadComprada`.
Calcula el costo total de la compra multiplicando estas variables y muestra el resultado en
la consola.*/

var precioProducto, cantidadComprada;

precioProducto = 10000;
cantidadComprada = 4;

console.log(`Costo total de la compra: $${precioProducto*cantidadComprada}ARS`);

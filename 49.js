/*49. crea una variable llamada `num1` y otra llamada `num2`. Escribe una expresión que
veri que si `num1` es mayor que `num2` y muestra el resultado (true o false) en la
consola.*/

var num1, num2;

num1 = 2;

num2 = 1;

expresion = num1 > num2;

console.log(`num1 > num2: ${expresion}`);
